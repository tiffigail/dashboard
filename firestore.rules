rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // All reads and writes require a signed-in Firebase user.
    // Since this is a single-user personal app, this is the right gate.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
